[phases.setup]
nixpkgsArchive = "https://github.com/nix-community/nixpacks/archive/refs/heads/main.zip"

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "npm ci", "npm run build"]

[start]
command = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
