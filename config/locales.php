<?php

return [
    // Supported locales for the app (code => human readable label)
    'supported' => [
        'en' => 'English',
        'ne' => 'नेपाली',
    ],
    // Whether to fallback to the other locale's content when the current
    // locale's value is empty. Set to `false` to avoid showing Nepali
    // content in English mode (or vice-versa) when the primary value is
    // missing.
    'fallback_to_other' => false,
];

